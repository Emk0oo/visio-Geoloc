const config = {
  SERVER_IP: "localhost", //Ip from server VPS
  SERVER_DOMAIN: "localhost", //Domain from server VPS
  SOCKET_PORT: 8080, //Port from server VPS
  map_id: "be31cd6f936aef12",//Map_id from GMAPS
};

export default config;
